{
  "name": "trpc-starter",
  "engines": {
    "node": ">=20.15.0",
    "pnpm": "^9.14.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@acme/prettier-config": "workspace:*",
    "prettier": "catalog:",
    "turbo": "^2.3.3",
    "typescript": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
